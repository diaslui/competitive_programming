#include <bits/stdc++.h>

using namespace std;

#define INF 1e9 * 10
#define endl "\n"
#define ll long long

int main(){
   ios::sync_with_stdio(0);
   cin.tie(0);

   int n; cin >> n;
   int v = 0;
   for (int i=0; i < n; i++){
    cin >> v[i];
    v = gcd(v, v[i]);
   }
    int ans = 0;
   for (int i=1; i * i <= v; i++){
   
     if (v % i == 0){
	ans++;
	}
  }
    cout << ans << "\n";

   return 0;
}

